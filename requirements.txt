# 📌 pin ช่วงเวอร์ชัน (19 ก.ค. 2026) — cloud เคย rebuild ด้วยไลบรารีใหม่แล้วพัง
#    (pandas ใหม่คุย DB ไม่เหมือนเดิม) · ขยับเพดานเมื่อทดสอบแล้วเท่านั้น
streamlit>=1.45.0,<2    # ≥1.37 fragment · ≥1.45 dropdown แพทย์พิมพ์เพิ่มได้
pandas>=2.0.0,<2.3
numpy>=1.24.0,<2
scikit-learn>=1.3.0,<1.6
plotly>=5.18.0,<6
openpyxl>=3.1.0,<4
streamlit-autorefresh>=1.0.1
xlsxwriter>=3.1.0,<4

# (anthropic ถูกถอด 4 ก.ค. 2026 — ใช้แค่ใน or_agent_demo.py ซึ่งย้ายเข้า _archive_legacy แล้ว)

# ML — prediction (โมเดล pkl เทรนด้วย xgboost 2.x / sklearn 1.x — ห้ามข้าม major)
xgboost>=2.0.0,<3
joblib>=1.3.0,<2
rapidfuzz>=3.0.0,<4
xlrd>=2.0.1,<3       # อ่านไฟล์ intraop .xls (HIS export)

# Supabase / PostgreSQL
psycopg2-binary>=2.9.9,<3
toml>=0.10.2
