streamlit>=1.37.0   # ⚡ ต้อง ≥1.37: บอร์ดใช้ st.fragment (rerun เฉพาะบอร์ด ไม่ทั้งแอป)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
plotly>=5.18.0
openpyxl>=3.1.0
streamlit-autorefresh>=1.0.1
xlsxwriter>=3.1.0

# AI Agent (or_agent_demo.py)
anthropic>=0.40.0

# ML — prediction + retrain/fine-tune
xgboost>=2.0.0
joblib>=1.3.0
rapidfuzz>=3.0.0
xlrd>=2.0.1          # อ่านไฟล์ intraop .xls (HIS export)

# Supabase / PostgreSQL
psycopg2-binary>=2.9.9
toml>=0.10.2
