# 📌 25 ก.ค. 2026: Streamlit Cloud บังคับ Python 3.14 แล้ว — ปลด upper pin
#    ให้ uv เลือกรุ่นที่มี wheel สำหรับ 3.14 (pin แน่นแล้ว build ค้าง)
#    ⚠️ ความเสี่ยงที่ต้องเฝ้า: pickle โมเดล (xgboost/sklearn ข้ามรุ่น) —
#    เช็ก chip AI: thesis_ML_v2 หลัง deploy ทุกครั้ง
streamlit>=1.45.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
plotly>=5.18.0
openpyxl>=3.1.0
streamlit-autorefresh>=1.0.1
xlsxwriter>=3.1.0

# ML — prediction
xgboost>=2.0.0
joblib>=1.3.0
rapidfuzz>=3.0.0
xlrd>=2.0.1          # อ่านไฟล์ intraop .xls (HIS export)

# Supabase / PostgreSQL
psycopg2-binary>=2.9.9
toml>=0.10.2
